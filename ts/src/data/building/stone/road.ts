import { spriteRefs } from "../../../asset/sprite.ts";

export const road = {
    id: "road",
    icon: spriteRefs.times,
    name: "Road",
    scale: 1,
    previewScale: 4,
    previewOffset: 0,
} as const;
