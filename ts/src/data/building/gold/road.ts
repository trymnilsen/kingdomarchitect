import { spriteRefs } from "../../../asset/sprite.ts";

export const road = {
    id: "road",
    icon: spriteRefs.empty_sprite,
    name: "Road",
    scale: 4,
    previewScale: 4,
    previewOffset: 0,
} as const;
