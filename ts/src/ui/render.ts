/*
export function renderUI(
    canvas: HTMLCanvasElement,
    rootComponent: FunctionComponent<any>,
): () => void {}

type UINode = UIElement & {};

function reconcile(oldNode: UINode, newElement: UIElement);
*/
