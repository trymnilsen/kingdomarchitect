import { describe, expect, test } from "@jest/globals";

describe("UIView", () => {
    test("Throws error if width is not valid", () => {
        expect(3).toBe(3);
    });
    test("Throws error if height is not valid", () => {
        expect(3).toBe(3);
    });
    test("Throws error if view is already added to another parent", () => {
        expect(3).toBe(3);
    });
    test("Throws error if view is added to itself", () => {
        expect(3).toBe(3);
    });
    test("can add children", () => {
        expect(3).toBe(3);
    });
    test("can remove children", () => {
        expect(3).toBe(3);
    });
    test("updates transform with parent value", () => {
        expect(3).toBe(3);
    });
    test("sets screenposition to offset if there is no parent", () => {
        expect(3).toBe(3);
    });
    test("updates transform of children after self", () => {
        expect(3).toBe(3);
    });
});
