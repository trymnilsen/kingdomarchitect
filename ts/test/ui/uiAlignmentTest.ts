import { describe, expect, test } from "@jest/globals";

describe("Camera", () => {
    test("Sets position rounded", () => {
        expect(3).toBe(3);
    });
    test("can convert from screenspace to tilespace", () => {
        expect(3).toBe(3);
    });
    test("can convert tilespace to screenspace", () => {
        expect(3).toBe(3);
    });
    test("translate moves camera", () => {
        expect(3).toBe(3);
    });
});
