import * as assert from "node:assert";

describe("uiScroll test", () => {
    it("Shows scroll buttons", () => {
        assert.equal(2, 2);
    });

    it("allows content to be layed out outside of measured width", () => {
        assert.equal(2, 2);
    });

    it("enables scroll buttons if there is overflowing content", () => {
        assert.equal(2, 2);
    });

    it("scrolls half a view port size at a time", () => {
        assert.equal(2, 2);
    });

    it("allows overflow only in orientation direction", () => {
        assert.equal(2, 2);
    });

    it("fill ui constraints of child will be set to viewport size", () => {
        assert.equal(2, 2);
    });
});
