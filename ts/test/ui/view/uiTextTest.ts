import { describe, expect, test } from "@jest/globals";

describe("UIText", () => {
    test("Measures text", () => {
        expect(3).toBe(3);
    });
    test("Aligns correctly", () => {
        expect(3).toBe(3);
    });
});
