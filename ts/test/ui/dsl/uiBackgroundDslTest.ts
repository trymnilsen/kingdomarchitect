import { describe, expect, test } from "@jest/globals";

describe("NinePatchBackground", () => {
    test("Creates a background with properties", () => {
        expect(3).toBe(3);
    });
    test("Sets the sides to a default value", () => {
        expect(3).toBe(3);
    });
    test("Sets a default scale value", () => {
        expect(3).toBe(3);
    });
});
