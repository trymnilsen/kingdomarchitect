import test from "ava";

test("add actor", (t) => {
    t.pass();
});

test("get actor", (t) => {
    t.pass();
});

test("onUpdate updates all actors", (t) => {
    t.pass();
});

test("onDraw updates all actors", (t) => {
    t.pass();
});
