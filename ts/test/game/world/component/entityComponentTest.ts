import * as assert from "node:assert";

describe("EntityComponent", () => {
    it("Can publish events", () => {
        assert.equal(2, 2);
    });
    it("Throws error if entity is not set", () => {
        assert.equal(2, 2);
    });
});
