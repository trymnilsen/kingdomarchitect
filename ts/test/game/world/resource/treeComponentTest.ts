import * as assert from "node:assert";
import { describe, it } from "node:test";

describe("TreeComponent", () => {
    it("can chop full tree", () => {
        assert.equal(2, 2);
    });

    it("can clear stub", () => {
        assert.equal(2, 2);
    });

    it("will regrow if stub", () => {
        assert.equal(2, 2);
    });
});
