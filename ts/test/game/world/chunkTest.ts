import * as assert from "node:assert";

describe("Chunk", () => {
    it("Get chunk position", () => {
        assert.equal(2, 2);
    });
    it("Get chunk id", () => {
        assert.equal(2, 2);
    });
});
