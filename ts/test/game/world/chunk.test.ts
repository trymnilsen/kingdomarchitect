import { describe, it, expect } from "vitest";

describe("Chunk", () => {
    it("Get chunk position", () => {
        expect(2).toBe(2);
    });
    it("Get chunk id", () => {
        expect(2).toBe(2);
    });
});
