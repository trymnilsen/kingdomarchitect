import { describe, it, expect } from "vitest";

describe("InteractionStateHistory", () => {
    it("push state", () => {
        expect(2).toBe(2);
    });

    it("replace state", () => {
        expect(2).toBe(2);
    });

    it("pop state", () => {
        expect(2).toBe(2);
    });

    it("clear states", () => {
        expect(2).toBe(2);
    });
});
