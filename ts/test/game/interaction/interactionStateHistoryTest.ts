import test from "ava";

test("push state", (t) => {
    t.pass();
});

test("replace state", (t) => {
    t.pass();
});

test("pop state", (t) => {
    t.pass();
});

test("clear states", (t) => {
    t.pass();
});
