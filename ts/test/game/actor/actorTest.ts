import { describe, expect, test } from "@jest/globals";

describe("Actor test", () => {
    test("Actor added should be added to actors", () => {
        expect(3).toBe(3);
    });
    test("start tick is set on first update if there is a job", () => {
        expect(3).toBe(3);
    });
    test("assigning same job to same actor throws error", () => {
        expect(3).toBe(3);
    });
    test("assigning job starts it and starts to listen for completion", () => {
        expect(3).toBe(3);
    });
    test("assigning job starts it and starts to listen for completion", () => {
        expect(3).toBe(3);
    });
});
