import { describe, expect, test } from "@jest/globals";

describe("Point", () => {
    test("zero point is not mutable", () => {
        expect(3).toBe(3);
    });
    test("add two points", () => {
        expect(3).toBe(3);
    });
    test("subtract two points", () => {
        expect(3).toBe(3);
    });
    test("change only x value", () => {
        expect(3).toBe(3);
    });
    test("change only y value", () => {
        expect(3).toBe(3);
    });
    test("invert point", () => {
        expect(3).toBe(3);
    });
    test("multiply point", () => {
        expect(3).toBe(3);
    });
    test("point equals", () => {
        expect(3).toBe(3);
    });
    test("distance between two points", () => {
        expect(3).toBe(3);
    });
    test("get adjacent points", () => {
        expect(3).toBe(3);
    });
    test("check if one point is adjacent to another", () => {
        expect(3).toBe(3);
    });
    test("two distant points are not adjacent", () => {
        expect(3).toBe(3);
    });
});
