import { describe, expect, test } from "@jest/globals";

describe("Bounds", () => {
    test("Is within rectangle", () => {
        expect(3).toBe(3);
    });
    test("top is outside rectangle", () => {
        expect(3).toBe(3);
    });
    test("left is outside rectangle", () => {
        expect(3).toBe(3);
    });
    test("right is outside rectangle", () => {
        expect(3).toBe(3);
    });
    test("bottom is outside rectangle", () => {
        expect(3).toBe(3);
    });
    test("zerobounds is not mutable", () => {
        expect(3).toBe(3);
    });
    test("gets range from axis", () => {
        expect(3).toBe(3);
    });
    test("offsets bounds to absolute coordinates", () => {
        expect(3).toBe(3);
    });
});
