import { assert } from "chai";

describe("Range test", () => {
    it("can pick random number in range", () => {
        assert.equal(2, 2);
    });

    it("can calculate distance of range", () => {
        assert.equal(2, 2);
    });
});
