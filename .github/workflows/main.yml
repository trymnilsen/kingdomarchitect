name: Build preview
on:
  push:
    branches:
      - main
      
jobs:
  compile:
    runs-on: ubuntu-latest
    steps:
      - name: Compile the source
        run: echo compile placeholder
  check-format:
    runs-on: ubuntu-latest
    steps:
      - name: Compile the source
        run: echo compile placeholder
  test:
    runs-on: ubuntu-latest
    needs: compile
    steps:
      - name: Compile the source
        run: echo compile placeholder
  bundle:
    runs-on: ubuntu-latest
    needs: compile
    steps:
      - name: Compile the source
        run: echo compile placeholder
  deploy-preview:
    runs-on: ubuntu-latest
    needs: [compile, bundle, test, check-format]
    steps:
      - name: Compile the source
        run: echo compile placeholder