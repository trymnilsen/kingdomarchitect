import { describe, it } from "node:test";
import assert from "node:assert";

describe("UiImage", () => {
    it("Creates a background with properties", () => {
        assert.strictEqual(1, 1);
    });
    it("Sets the sides to a default value", () => {
        assert.strictEqual(1, 1);
    });
    it("Sets a default scale value", () => {
        assert.strictEqual(1, 1);
    });
});
