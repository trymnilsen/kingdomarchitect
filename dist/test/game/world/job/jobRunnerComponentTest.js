import { assert } from "chai";
describe("JobRunnerComponent", ()=>{
    it("can get active job", ()=>{
        assert.equal(2, 2);
    });
    it("has active job is correct", ()=>{
        assert.equal(2, 2);
    });
    it("can assign job", ()=>{
        assert.equal(2, 2);
    });
    it("will request new job on completion", ()=>{
        assert.equal(2, 2);
    });
});
